{
  "name": "borders",
  "root": "packages/borders",
  "sourceRoot": "packages/borders/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "pnpm --filter borders build"
      }
    }
  }
}
